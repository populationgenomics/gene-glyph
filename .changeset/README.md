# Changesets

Add a changeset with `npm run changeset` whenever you make a user-facing change to the published package. The release workflow opens a "Version Packages" PR that, when merged, publishes to npm.
